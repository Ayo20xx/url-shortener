# URL Shortener
A REST API built with FastAPI that shortens long URLs.

## Tech Stack
- FastAPI
- Python

## Endpoints
- `POST /url-shorten` - Shorten a URL
- `GET /{short_code}` - Redirect to original URL
